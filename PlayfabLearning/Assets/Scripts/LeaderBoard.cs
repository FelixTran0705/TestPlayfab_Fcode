using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeaderBoard : MonoBehaviour
{

    protected List<GameObject> listCard = new List<GameObject>();


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //protected GameObject SpawnContentUnit(GameObject obj)
    //{
    //    GameObject card = Instantiate(obj, contentList.transform);
    //    listCard.Add(card);
    //    return card;
    //}
}
